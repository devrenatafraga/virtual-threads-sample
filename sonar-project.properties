# SonarCloud project configuration
# Note: Most configurations are handled in build.gradle.kts
# This file is kept for compatibility and environment-specific overrides

sonar.projectKey=devrenatafraga_virtual-threads-sample
sonar.organization=devrenatafraga
sonar.projectName=Virtual Threads Sample
sonar.projectVersion=1.0

# Source encoding
sonar.sourceEncoding=UTF-8

# Java version
sonar.java.source=21

# Coverage reports paths (fallback if not specified in Gradle)
sonar.coverage.jacoco.xmlReportPaths=build/reports/jacoco/test/jacocoTestReport.xml
